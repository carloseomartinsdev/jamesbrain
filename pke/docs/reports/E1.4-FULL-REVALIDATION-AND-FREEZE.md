# E1.4 — Full Revalidation & Freeze Report

**Project:** `pkeagent`  
**Date:** 2026-09-04  
**Authorization:** E1.4 APPROVED · NEW DOMAIN SEMANTICS FORBIDDEN  
**Environment:** Python 3.13 · `PYTHONPATH=src;%APPDATA%\Python\Python313\site-packages`

---

## Final recommendation

```text
E1.1 COMPLETE
E1.2 COMPLETE
E1.3 COMPLETE
E1.4 COMPLETE

PKE E1
EVERYDAY KNOWLEDGE FOUNDATION
FROZEN
```

Token: `E1_EVERYDAY_KNOWLEDGE_FOUNDATION_FROZEN`

Automated gates required by §24 are **PASS**. Product API smoke is covered by
frozen Product suites against `/api/v1/messages`. Live Portal JAMES smoke is an
operator Beta-resume step (presentation proxy; no Portal semantic ownership).

---

## Full regression result

```text
pytest tests/ -m "not live"
→ 5137 passed, 8 deselected (live), 1 warning
→ exit 0
```

Prior E1.4 draft had **37 failed / 5100 passed**; failures were fixed (below) and
full suite re-run to green. Not classified as `TEST_ENVIRONMENT_BLOCKED`.

Baseline comparison: P-R frozen baseline was ~5097 non-live; E1.4 exceeds it.

---

## Focused regression result

| Suite | Result |
|-------|--------|
| Layering + clock (`test_persist_does_not_import_application`, `test_application_does_not_call_wall_clock`) + `clarification_coverage` + `e1_principal` | **333 passed** |
| `persist_migrations` + `e1_*` + `api` + `product` | **97 passed** |
| `product_v1_freeze` + `e1_everyday` + `e1_clarification` + `clarification_coverage` | **678 passed** |

Contracts revalidated via existing E1 suites (no new domain semantics added):

- PrincipalBinding lazy / idempotent / AuthUser ≠ Entity / cross-user isolation
- Self repair positives & negatives
- Vehicle ownership graph (not flattened on principal)
- Cross-conversation Knowledge queries (name + vehicle + color)
- Capability matrix CLARIFY / UNSUPPORTED / SAFE_ABSTAIN / AUTO_EXECUTE
- Attribute routing safety (registry-controlled; arbitrary dims do not commit)

---

## Migration result

| Check | Result |
|-------|--------|
| Fresh DB → schema **v11** | PASS (canonical runner / open path) |
| Existing v10 → migrate → v11 | PASS (`v10_to_v11` in runner chain) |
| Additive `principal_bindings` only | PASS (no artificial bindings on migrate) |
| Knowledge rows reinterpretation | none (migration is DDL-only) |
| `STORAGE_SCHEMA_VERSION` | `"11"` |

---

## Product API smoke

```text
POST /api/v1/messages
```

Covered by `tests/api/` and `tests/product_v1_freeze/` (commit / query /
clarification / unsupported / safe failure / ownership / idempotency).
Public DTO remains schema **1.4** compatible.

**Result:** PASS

---

## JAMES smoke

| Item | Status |
|------|--------|
| Semantic path under test | Same Product `/api/v1/messages` DTO JAMES proxies |
| Automated Product/E1 cross-conversation proofs | PASS |
| Live Portal click-through this session | **NOT EXECUTED** (operator on Beta resume) |

Classification: not `TEST_ENVIRONMENT_BLOCKED` for Engine/Product; Portal live
smoke deferred to Beta reopen checklist (no special JAMES logic expected).

---

## Regressions found

### R1 — Persist → Application layering (E1.1)

`SqlUnitOfWork` imported `SqlPrincipalBindingRepository` from `application`,
breaking `test_persist_does_not_import_application`.

### R2 — Wall-clock in Principal bootstrap (E1.1)

`ensure_principal_entity` used `datetime.now`, breaking Clock authority
(`test_application_does_not_call_wall_clock`).

### R3 — Clarification corpus drift (E1.2)

`attr_brand_gap` used `toyota` / brand, now registry-materializable → cases
resolved instead of remaining unresolved (`ATTR_DIM_*` coverage).

---

## Corrections made (E1.4-allowed only)

| Fix | Nature |
|-----|--------|
| Move `SqlPrincipalBindingRepository` → `persist/sqlite/repositories.py`; UoW imports persist only | E1.1 layering regression |
| `PrincipalBindingService.ensure_principal_entity(..., *, now: datetime)` required; no wall-clock fallback | E1.1 Clock contract |
| Clarification corpus: unsupported surface `aura blorp`; UNKNOWN answer `signo` | Test outdated by explicit E1.2 registry |

No new attributes, relations, primitives, domains, or inference mechanisms.

---

## Remaining known gaps (record / defer)

| Gap | Classification |
|-----|----------------|
| `Pintei meu carro de preto.` historical color event path | capability gap if still unsupported — do not expand E1 |
| Zodiac / invented dimensions | UNSUPPORTED (by design) |
| Family / work / preferences / world ontology | POST_E1 |
| Live JAMES Portal operator smoke | Beta resume checklist |

---

## Freeze artifacts

- ADR: `docs/decisions/0079-e1-everyday-knowledge-foundation-freeze.md`
- Freeze note: `docs/PKE-E1-FREEZE.md`
- Plan: `docs/PKE-E1-EVERYDAY-KNOWLEDGE-PLAN.md` (E1.4 COMPLETE)

Tag/commit: follow repo convention when operator commits (not auto-pushed here).
