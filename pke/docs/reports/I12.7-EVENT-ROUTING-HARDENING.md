# I12.7 — Event Routing & Canonicalization Preservation (Closure)

**Status:** CLOSED  
**Date:** 2026-09-03  
**ADR:** [0058-event-routing-downstream-preservation.md](../decisions/0058-event-routing-downstream-preservation.md)

## Executive

| Question | Answer |
|----------|--------|
| WAS_THE_DOWNSTREAM_EVENT_LOSS_ROOT_CAUSE_IDENTIFIED? | **YES** |
| IS_EVENT_ROUTING_NOW_DETERMINISTICALLY_INFORMATION_PRESERVING? | **YES** |
| CAN_THE_FIX_INVENT_EVENTS_NOT_PRESENT_IN_THE_PROPOSAL? | **NO** |
| WHEN_RAW_PROPOSAL_CONTAINS_EVENT_AND_MEASUREMENT, ARE_BOTH_NOW_PRESERVED_POST_ENGINE? | **YES** |
| DOES_MP5_REMAIN_MEASUREMENT_ONLY? | **YES** |
| DOES_KNOWLEDGE_CORE_V1_REMAIN_FROZEN? | **YES** |

## Root cause report

```text
EVENT_LOSS_STAGE = S6_MATERIALIZATION_INPUT (persistability → wire gate)
EVENT_LOSS_COMPONENT = assess_persistability._assess_event
EVENT_LOSS_MECHANISM = unresolved catalog action+event_type marked critical →
  wire_allowed=False → resolution_to_wire_ingest drops Event block → measurement-only intent

WHY_I12_5_DID_NOT_FULLY_ELIMINATE_IT =
  I12.5 fixed router/assertion collection only; persistability still rejected
  compositional partial Events before wire materialization.
```

## Safety (§6)

```text
CAN_EVENT_PRESERVATION_CREATE_AN_EVENT_WHEN_NO_EVENT_SEMANTICS_EXIST_IN_THE_PROPOSAL?
NO
```

## MS18

```text
AT_WHICH_STAGE_DOES_MS18_LOSE_EVENT? (before fix)
S6_MATERIALIZATION_INPUT / _assess_event (event_identity_incomplete)

After fix: Event preserved through S8 with partial type event.intent.
```

## Stage matrix (anchors)

See `docs/reports/I12.7-STAGE-MATRIX-ANCHORS.json` (generated by tests).

| Case | Proposal | Assertions | Resolution | Materialization | Post |
|------|----------|------------|------------|-----------------|------|
| MP1 | E+M | E+M | E+M | E+M | E+M |
| MP2 | E+M | E+M | E+M | E+M | E+M |
| MP3 | E+M | E+M | E+M | E+M | E+M |
| MP4 | E+M | E+M | E+M | E+M | E+M |
| MP5 | M | M | M | M | M |
| MS18 | E+M | E+M | E+M | E+M | E+M |

## Deterministic metrics

```text
TOTAL_CASES (explicit Event corpus) = 103
EXPLICIT_EVENT_PRESERVATION_RATE = 100%
EXPLICIT_MEASUREMENT_PRESERVATION_RATE = 100%

EXPLICIT_EVENT_LOST_DOWNSTREAM = 0
UNSUPPORTED_EVENT_ADDITION = 0
DUPLICATE_EVENT_CREATED = 0
MEASUREMENT_LOST_DUE_TO_EVENT_FIX = 0
```

## Regression

```text
baseline = 3037 passed
new tests = 112 (tests/event_routing_hardening/)
total passed = 3149
failed = 0
live deselected = 8
```

## Focused live

Runner: `python -m tests.event_routing_hardening.run_i127_focused_live`  
Artifacts: `docs/reports/i127_artifacts/`  
Execute when `DEEPSEEK_API_KEY` available (development only).

## Debt reassessment

```text
INTERPRETER-EVENT-01 = PARTIALLY_MITIGATED
INTERPRETER-STATE-01 = OPEN
INTERPRETER-RELATION-01 = OPEN
LIVE-INTERPRETER-RELIABILITY-01 = PARTIALLY_MITIGATED
```

```text
AFTER_EVENT_ROUTING_HARDENING, HIGHEST_REMAINING_ENGINE_V1_BLOCKER = EVENT_INTERPRETATION
IS_THE_CURRENT_EFFECTIVE_LANGUAGE_BOUNDARY_NOW_SAFE_ENOUGH_FOR_ENGINE_V1? = YES
  (safe ≠ fully reliable — raw model may still omit Event)
```

## Recommendation

```text
I12.7_CLOSE_PROCEED_TO_TARGETED_INTERPRETER_HARDENING
```

## Change summary

- `src/pke/interpretation/semantic/persistability.py` — compositional partial Event
  persistability; `event.intent` wire fallback; occurrence aligned with I12.5 predicate
- `tests/event_routing_hardening/` — 112 tests, stage trace, corpus, focused live runner
- `tests/measurement_storage/test_non_materialized_preservation.py` — R3 updated for I12.7 contract

## Frozen constraints (confirmed)

```text
prompt before = v4 · after = v4
SemanticProposal = unchanged
Wire = unchanged
RetryPolicy = unchanged
Correction Guard = unchanged (CAPTURED_S4_BLOCK_RATE = 100%)
Knowledge Core v1 = FROZEN · schema v10 · CORE 65
holdout = untouched
```
