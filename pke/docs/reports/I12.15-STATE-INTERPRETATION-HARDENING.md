# I12.15 — State Interpretation Characterization & Targeted Hardening

## Executive

| Question | Answer |
|----------|--------|
| IS_STATE_INTERPRETATION_CHARACTERIZATION_COMPLETE? | **YES** |
| WHAT_IS_THE_DOMINANT_STATE_FAILURE? | **CANONICALIZATION_FAILURE** |
| WAS_A_DETERMINISTIC_ENGINE_STATE_DEFECT_FOUND? | **YES** (false CLARIFY on uncanonicalized expression) |
| WAS_SAFE_STATE_HARDENING_IMPLEMENTED? | **YES** (clarification hygiene only) |
| DID_STATE_HARDENING_REQUIRE_RAW_TEXT_REINTERPRETATION? | **NO** |
| DID_STATE_HARDENING_CREATE_FALSE_STATE_ASSERTIONS? | **NO** |
| DOES_KNOWLEDGE_CORE_V1_REMAIN_FROZEN? | **YES** |
| STATE_INTERPRETATION_HARDENING | **PARTIAL** |
| CAN_STATE_RELIABILITY_BE_IMPROVED_WITHOUT_REINTERPRETING_RAW_OR_INVENTING? | **PARTIALLY** |
| IS_LANGUAGE_BOUNDARY_SAFE_ENOUGH_FOR_ENGINE_V1? | **NO** |
| IS_INTERPRETATION_PLUS_BOUNDED_RECOVERY_RELIABLE_ENOUGH_FOR_ENGINE_V1? | **NO** |

## Central answers

```text
WHAT_IS_THE_DOMINANT_CAUSE_OF_REMAINING_STATE_FAILURES?
= CANONICALIZATION_FAILURE

CAN_EXISTING_SEMANTICPROPOSAL_EXPRESS_A_COMPLETE_STATE_ASSERTION?
= YES

ACTIVE_PROMPT_AFTER_I12_15 = v4
(no candidate prompt promoted)
```

## Architecture

```text
STATE_INTERPRETATION_AUTHORITY =
  semantic router + resolve_proposal / ContextualAlias STATE + persistability

STATE_DIMENSION_RESOLUTION_AUTHORITY =
  ContextualAlias.dimension_key (+ ConceptCatalog.state_dimensions)

STATE_VALUE_RESOLUTION_AUTHORITY =
  ContextualAlias.value_key (+ ConceptCatalog.state_values)

STATE_CLARIFICATION_AUTHORITY =
  CapabilityStrategy (eligibility)
  + ClarificationRecoveryService (recovery; state_value only)
```

## Characterization (frozen)

Artifact: `docs/reports/i1215_artifacts/I12.15-STATE-FAILURE-LEDGER.json`

Source: I12.11 `baseline_deepseek_chat`, `category=state`, re-evaluated on current Engine
(not stale `useful_capture` labels).

| Metric | Value |
|--------|------:|
| TOTAL_STATE_CASES | 13 |
| TOTAL_STATE_RUNS | 39 |
| STATE_CORRECT (autonomous commit) | 12 |
| VALID_STATE_CANONICAL_UNRESOLVED | 27 |
| VALID_STATE_LOST_DOWNSTREAM | 0 |
| VALID_PROPOSAL_STATE_OMITTED | 0 |
| VALID_PROPOSAL_WRONG_PRIMITIVE | 0 |
| STATE_AUTONOMOUS_CAPTURE_RATE | 30.8% |
| STATE_RECOVERABLE_GAP_RATE (after fix) | 0.0% |
| STATE_TOTAL_CAPTURE_AFTER_BOUNDED_RECOVERY | 30.8% |

### Failure matrix

| Failure | Count | First causal stage | Model-side? | Engine-side? | Recoverable? |
|---|---:|---|---:|---:|---:|
| omitted | 0 | — | — | — | — |
| incomplete (empty value) | 0 in live state set | S6 | — | — | yes via state_value |
| wrong primitive | 0 | — | — | — | — |
| canonical unresolved | 27 | S4 | yes (vocab) | clarify hygiene fixed | **no** without ontology |
| entity failure | 0 | — | — | — | — |
| downstream loss | 0 | — | — | — | — |

Non-CORE expressions seen: `ligado`, `on`/`off`, `paid`, `ativo`/`inativo`,
`connected`/`disconnected`, `full`/`cheio`, `indisponível`, …

### Decision gate

Ontology expansion forbidden → **no capture-raising fix** without Core reopen.

Deterministic defect (false clarify) **was** fixable without inventing semantics → implemented.

## Hardening implemented

1. `persistability._assess_state`: present `state_expression` + unresolved →
   `state_value_canonical_unresolved`
2. `execution_readiness`: map to `REASON_STATE_CANONICAL_UNRESOLVED` (not clarifiable)
3. `capability_strategy`: SAFE_ABSTAIN for that reason
4. Fix State/measurement/attribute `question_key` strings

**Not** implemented: prompt experiment, ontology seeds, raw lexical State detector.

## Clarification matrix (State)

| State gap | Detectable | Clarification eligible | Recovery supported | Outcome |
|---|---:|---:|---:|---|
| missing entity | limited | no (State persistability) | entity_reference if other prim | unchanged |
| missing dimension | yes | medium (policy) | **NO** (`state_dimension`) | unsupported |
| missing value | yes (empty expr) | yes | **YES** | clarify → recover |
| ambiguous entity | resolver | existing | entity_reference | unchanged |
| ambiguous dimension | rare | — | no | abstain |
| unknown canonical value | yes | **NO** | no | safe abstain |

```text
WHAT_EXACT_STATE_DIMENSION_GAPS_ARE_RECOVERABLE_TODAY?
= none
(I12.14 dimension recovery = measurement_dimension + attribute_dimension only)
```

## Deterministic corpus

`tests/state_interpretation_hardening/` — **221** cases

Safety targets: ATTRIBUTE/RELATION/EVENT/MEASUREMENT falsely → State = **0**;
false clarify on canonical gap = **0**; raw reinterpret = **0**.

## Regression

```text
pytest tests/ -m "not live"
3828 passed, 0 failed, 8 live deselected
(baseline was 3603)
```

## Debt reassessment

```text
INTERPRETER-STATE-01 = PARTIALLY_MITIGATED
INTERPRETER-RELATION-01 = OPEN
INTERPRETER-EVENT-01 = PARTIALLY_MITIGATED
CLARIFICATION_COVERAGE = PARTIALLY_MITIGATED
LIVE-INTERPRETER-RELIABILITY-01 = PARTIALLY_MITIGATED
```

## Highest blocker

```text
RELATION_INTERPRETATION
```

(State remaining debt is primarily frozen-CORE vocabulary coverage, which requires
`STATE_HARDENING_REQUIRES_ONTOLOGY_EXPANSION` — separate from Engine routing.)

## Recommendation

```text
I12.15_CLOSE_PROCEED_TO_RELATION_INTERPRETATION_HARDENING
```

## Explicit confirmation

State remains semantically distinct from Event, Relation, Attribute and Measurement.

Observed State does not imply a causal Event.

State Dimension remains distinct from State Value.

No State assertion is created from raw-text heuristics.

No missing State semantics are invented.

Explicit structured State semantics are preserved.

Safe unresolved State semantics remain unresolved rather than being forced into an unrelated canonical concept.

Clarification remains bounded to detectable user-answerable gaps.

CapabilityStrategy remains clarification eligibility authority.

ClarificationRecoveryService remains recovery authority.

StateResolver remains epistemic State authority.

No prompt/model/provider change is made silently.

SemanticProposal unchanged.
Wire unchanged.
RetryPolicy unchanged.

Knowledge Core v1 remains FROZEN.
schema v10.
CORE 65.

holdout untouched.

**PARAR.**
